# 작업 계획서

## 개요
C 분석 이슈 후처리 중복 제거 — 함수별 개별 LLM 호출로 발생하는 동일 패턴 이슈를 합산 후 자동 병합.

## 중복 패턴 (29건 중 14건 노이즈)

| 패턴 | 중복 건수 | 대표 | 제거 대상 |
|------|----------|------|-----------|
| strncpy 널 종료 | 9건 | C-002 대표 1건 | 8건 병합 |
| 스레드 안전성 | 2건 | - | 전부 제거 (Proframe 단일스레드) |
| NULL 체크 | 2건 | C-011 대표 1건 | 1건 병합 |
| svc_cnt 경계 | 2건 | C-019 대표 1건 | 1건 병합 |
| DB 배열 경계 | 2건 | C-016 대표 1건 | 1건 병합 |
| ix 미초기화 (for초기화됨) | 1건 | - | 제거 (오탐) |

## 진행 예정 Task

### T29: C 분석 이슈 후처리 중복 제거

#### T29.1: `_deduplicate_issues()` 구현 → 대상: `mider/agents/c_analyzer.py`
- 삽입 위치: `all_issues` 합산 후, `issue_id` 재번호 전
- 중복 판정: title 키워드 매칭 (정규화 후 비교)
  - strncpy/strlcpy/널 종료 → 1건으로 병합
  - 스레드/동기화/mutex/race → 전부 제거
  - NULL 체크/검증 → 1건으로 병합
  - 동일 변수명 + 동일 패턴 (svc_cnt 경계, DB 배열 경계) → 1건으로 병합
- 병합 방식: severity 가장 높은 것 유지 + description에 "(외 N곳 동일 패턴)" 추가
- 스레드 안전성 이슈는 category="performance" + title에 "스레드/동기화" → 자동 제거

#### T29.2: 이슈 로그 작성 → 대상: `docs/issue-log/007-c-duplicate-issues.md`

#### T29.3: 단위 테스트 → 대상: `tests/test_agents/test_c_analyzer.py`
- strncpy 5건 → 1건 병합 확인
- 스레드 이슈 2건 → 0건 제거 확인
- 서로 다른 이슈는 병합 안 됨 확인

---

## 일정 요약
| Task | 의존성 | 상태 |
|------|--------|------|
| T1~T28, T19 | - | ✅ 완료 |
| T29 | T28 | **다음** — 이슈 중복 제거 |
| T15 | T29 | 대기 (마지막) |
